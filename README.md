# SpringBoot-with-React-js

### To Build React code ###
npm run build ->it will build the code and push it under src/main/resources/public/ folder

### To Build Spring Boot code ###
.\mvnw install  -> it will compile build and generate the jar

Test Github Checks for Pull Request
